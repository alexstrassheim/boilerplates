http:
   routers:
      to-dispatcharr:
         rule: 'Host(`tv2.{{ env "MY_DOMAIN" }}`)'
         entryPoints:
            - websecure
         service: dispatcharr

   services:
      dispatcharr:
         loadbalancer:
            servers:
               - url: http://192.168.178.3:9191

